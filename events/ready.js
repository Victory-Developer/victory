const Discord = require('discord.js');
const prefix = process.env.PREFIX;

module.exports = client => {
client.user.setActivity('Victory', {type: 'WATCHING'}) 
client.user.setActivity('Yakında  ! 7/24 Aktif !', {type: 'WATCHING'});


}